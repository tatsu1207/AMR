services:
  amr-predictor:
    build: .
    ports:
      - "8080:8000"
    volumes:
      - amr-uploads:/opt/amr/uploads
    environment:
      - MAX_CONCURRENT_JOBS=3
      - CLEANUP_HOURS=24
      - MAX_FILE_SIZE_MB=100
    restart: unless-stopped

volumes:
  amr-uploads:
